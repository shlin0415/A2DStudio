name: 构建并发布

on:
  workflow_dispatch:
    inputs:
      release_title:
        description: 'Release 显示标题（留空则使用标签名）'
        required: false
        type: string
      prerelease:
        description: '是否为预发布版'
        required: false
        default: false
        type: boolean

# 设置权限，允许创建 Release 和上传资产
permissions:
  contents: write

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      tag_name: ${{ steps.check_tag.outputs.tag_name }}
      is_prerelease: ${{ github.event.inputs.prerelease }}
      release_title: ${{ steps.set_title.outputs.release_title }}
      platforms_json: ${{ steps.set_platforms.outputs.platforms_json }}
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 验证当前 ref 是否为标签
        id: check_tag
        run: |
          TAG=$(git describe --exact-match --tags HEAD 2>/dev/null || true)
          if [ -z "$TAG" ]; then
            echo "错误：当前 ref 不是一个标签。请在下拉列表中选择一个标签再触发工作流。"
            exit 1
          fi
          echo "tag_name=$TAG" >> $GITHUB_OUTPUT
          echo "当前标签：$TAG"

      - name: 设置 Release 标题
        id: set_title
        run: |
          TITLE="${{ github.event.inputs.release_title }}"
          if [ -z "$TITLE" ]; then
            TITLE="${{ steps.check_tag.outputs.tag_name }}"
          fi
          echo "release_title=$TITLE" >> $GITHUB_OUTPUT

      - name: 设置固定平台列表
        id: set_platforms
        run: |
          # 默认构建所有平台：windows, linux-amd64, linux-aarch64
          echo "platforms_json=['windows','linux-amd64','linux-aarch64']" >> $GITHUB_OUTPUT

  build:
    needs: prepare
    runs-on: ${{ matrix.os }}
    timeout-minutes: 120
    env:
      PYTHONUTF8: 1
    strategy:
      matrix:
        platform: ${{ fromJSON(needs.prepare.outputs.platforms_json) }}
        include:
          - platform: windows
            os: windows-latest
            upx_url: https://github.com/upx/upx/releases/download/v5.1.1/upx-5.1.1-win64.zip
            archive_ext: 7z
            archive_cmd: 7z a -t7z -mx9 -md=128m -mmt=4 ../${{ github.event.repository.name }}-${{ needs.prepare.outputs.tag_name }}-windows.7z .
          - platform: linux-amd64
            os: ubuntu-latest
            upx_url: https://github.com/upx/upx/releases/download/v5.1.1/upx-5.1.1-amd64_linux.tar.xz
            archive_ext: tar.xz
            archive_cmd: XZ_OPT="-9 -T0" tar -cJf ../${{ github.event.repository.name }}-${{ needs.prepare.outputs.tag_name }}-linux-amd64.tar.xz  .
          - platform: linux-aarch64
            os: ubuntu-24.04-arm
            upx_url: https://github.com/upx/upx/releases/download/v5.1.1/upx-5.1.1-arm64_linux.tar.xz
            archive_ext: tar.xz
            archive_cmd: XZ_OPT="-9 -T0" tar -cJf ../${{ github.event.repository.name }}-${{ needs.prepare.outputs.tag_name }}-linux-aarch64.tar.xz .

    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.tag_name }}
          lfs: true

      - name: 设置 Python 环境（使用 uv）
        uses: astral-sh/setup-uv@v3
        with:
          version: 'latest'

      - name: 安装项目依赖
        run: |
          uv sync
          uv run main.py install emo

      - name: 安装 Nuitka 编译器
        run: uv pip install nuitka

      - name: 设置 Node.js 环境
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: 安装 pnpm
        run: npm install -g pnpm

      - name: 构建前端
        working-directory: frontend_vue
        run: |
          pnpm install
          pnpm build

      - name: 安装 UPX
        shell: bash
        run: |
          if [[ "${{ matrix.platform }}" == windows* ]]; then
            curl -L -o upx.zip "${{ matrix.upx_url }}"
            unzip upx.zip
            # 获取解压出的目录名
            UPX_DIR=$(ls -d upx-*/)
            # 获取 upx.exe 的绝对路径
            UPX_PATH=$(realpath "$UPX_DIR/upx.exe")
            echo "UPX_BIN=$UPX_PATH" >> $GITHUB_ENV
          else
            curl -L -o upx.tar.xz "${{ matrix.upx_url }}"
            tar -xf upx.tar.xz
            UPX_DIR=$(ls -d upx-*/)
            # 移动到标准位置并记录路径
            sudo mv "$UPX_DIR/upx" /usr/local/bin/upx
            echo "UPX_BIN=/usr/local/bin/upx" >> $GITHUB_ENV
          fi

      - name: 使用 Nuitka 编译
        shell: bash
        env:
          CFLAGS: '-flto=thin -O2'
          CXXFLAGS: '-flto=thin -O2'
        run: |
          NUMERIC_VERSION=$(echo ${{ needs.prepare.outputs.tag_name }} | grep -oE '[0-9]+\.[0-9]+\.[0-9]+')

          if [[ "${{ matrix.platform }}" == linux* ]]; then
            echo "yes" | uv run nuitka --clang --standalone --enable-plugins=upx --upx-binary=$UPX_BIN --jobs=4 --lto=yes --low-memory --assume-yes-for-downloads --linux-icon=ling_chat/static/game_data/resources/lingchat.png --output-filename=LingChat --product-name=${{ github.event.repository.name }}-${{ needs.prepare.outputs.tag_name }} --file-version="$NUMERIC_VERSION" --product-version="$NUMERIC_VERSION" --company-name="LingChat Studio" --show-memory main.py
          else
            echo "yes" | uv run nuitka --clang --standalone --enable-plugins=upx --upx-binary=$UPX_BIN --jobs=4 --lto=yes --low-memory --assume-yes-for-downloads --windows-icon-from-ico=ling_chat/static/game_data/resources/lingchat.ico --output-filename=LingChat --product-name=${{ github.event.repository.name }}-${{ needs.prepare.outputs.tag_name }} --file-version="$NUMERIC_VERSION" --product-version="$NUMERIC_VERSION" --company-name="LingChat Studio"  --show-memory main.py
          fi

      - name: 整理文件（准备打包）
        shell: bash
        run: |
          ls -la main.dist
          rm -rf ling_chat/static/frontend
          mkdir -p ling_chat/static/frontend
          cp -r frontend_vue/dist/* ling_chat/static/frontend/

          cp .env.example main.dist/.env
          cp LICENSE main.dist/LICENSE
          cp version main.dist/version

          mkdir main.dist/ling_chat
          mv ling_chat/static main.dist/ling_chat
          mv ling_chat/third_party main.dist/ling_chat

      - name: 打包
        working-directory: main.dist
        run: ${{ matrix.archive_cmd }}

      - name: 上传构建产物（供后续 release 使用）
        uses: actions/upload-artifact@v7
        with:
          name: package-${{ matrix.platform }}
          path: ${{ github.event.repository.name }}-${{ needs.prepare.outputs.tag_name }}-${{ matrix.platform }}.${{ matrix.archive_ext }}
          archive: false

  # 发布到 GitHub Release
  release:
    needs: [prepare, build]
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ needs.prepare.outputs.tag_name }}

      - name: 下载所有平台的构建包
        uses: actions/download-artifact@v8
        with:
          path: ./artifacts
          merge-multiple: true
          skip-decompress: true

      - name: 设置 Python 环境
        uses: astral-sh/setup-uv@v3
        with:
          version: 'latest'

      - name: 安装modelscope
        run: uv tool install modelscope

      - name: 创建或更新 Release 并上传所有资产
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          MODELSCOPE_ACCESS_TOKEN: ${{ secrets.MODELSCOPE_ACCESS_TOKEN }}
        run: |
          set -e
          TAG="${{ needs.prepare.outputs.tag_name }}"
          TITLE="${{ needs.prepare.outputs.release_title }}"
          PRERELEASE="${{ needs.prepare.outputs.is_prerelease }}"

          # 检查或创建 Release
          if gh release view "$TAG" >/dev/null 2>&1; then
            echo "Release $TAG 已存在，将上传资产到现有 Release"
          else
            echo "Release $TAG 不存在，正在创建..."
            gh release create "$TAG" --title "$TITLE" --prerelease="$PRERELEASE" --notes "自动构建的 Release"
          fi

          # 获取所有文件（包括子目录中的文件）
          echo "正在收集 artifacts 目录下的所有文件..."
          mapfile -t files < <(find ./artifacts -type f)

          if [ ${#files[@]} -eq 0 ]; then
            echo "错误：artifacts 目录下没有文件"
            exit 1
          fi

          echo "找到 ${#files[@]} 个文件待上传："
          printf '%s\n' "${files[@]}"

          # 逐个上传所有文件
          for file in "${files[@]}"; do
            echo "正在上传 $file ..."
            gh release upload "$TAG" "$file" --clobber
            echo "✓ 上传完成：$file"
          done

          for file in "${files[@]}"; do
            echo "正在上传modelcope: $file ..."
            ms upload lingchat-research-studio/LingChat-release-mirror "$file" ./$TAG/$file --token ms-928ef9e5-6136-4248-acb8-1abd928d3e21 #别想偷了，临时的，构建完就删除
            echo "✓ 上传完成：$file"
          done

          echo "✅ 所有 ${#files[@]} 个资产上传完成"
